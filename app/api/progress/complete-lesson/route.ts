import { NextResponse } from "next/server";

// Marks a lesson complete for the logged-in user. Implemented in Phase 3.
export async function POST() {
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}
