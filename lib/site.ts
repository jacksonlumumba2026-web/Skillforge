// Stable production domain for canonical URLs, OG tags, and the sitemap.
// Not read from NEXT_PUBLIC_SITE_URL because that's only ever been set to
// localhost for local dev — update this if the custom domain changes.
export const SITE_URL = "https://skillforge-delta-nine.vercel.app";
